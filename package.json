{
  "name": "codex-usage-dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node serve.js",
    "start:opencode": "opencode serve",
    "refresh": "node scripts/refresh.js",
    "fetch:opencode": "node scripts/fetch-opencode.js",
    "share:build": "node scripts/build-share.js",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "ccusage": "^20.0.20"
  }
}
